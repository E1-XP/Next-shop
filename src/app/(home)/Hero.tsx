import * as React from "react";

const HeroSection = () => {
  return (
    <section className="h-screen -mt-[72px] pt-[72px] bg-red-300">
      <h2>ok</h2>
    </section>
  );
};

export default HeroSection;
